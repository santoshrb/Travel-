<table id="example2" class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>  
      <th>Code</th>
      <th>Name</th>
      <th>Description</th>
      <th>Status</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.code %></td>
        <td><%= role.name %></td>
        <td><%= role.description %></td>
        <td><%= role.is_confirm ? "Active" : "Inactive" %></td>
        <td>
            <%= link_to '', role,class: 'btn btn-default btn-xs fa fa-eye' %>
            <%= link_to '', edit_role_path(role),class: 'btn btn-success btn-xs fa fa-edit' %>
            <%= link_to '',  role, method: :delete, data: { confirm: 'Are you sure?' },class: 'btn btn-danger btn-xs fa fa-close' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Role', new_role_path %>
